import SwiftUI

struct ThemeDark: ThemeStyle {
    var backgroundViewColor: Color = Color(red: 10 / 255, green: 10 / 255, blue: 10 / 255)
    var backgroundBlockColor: Color = .black
    var headerBlockTextColor:Color = .white
    var errorTextColor:Color = .red
    var fieldTextColor:Color = .white
    var secondaryIconColor:Color = .white
    var baseTextColor:Color = .white
    
    var backgroundColor: Color = .black
    var textColor: Color = .white
    var primaryButtonColor: Color = .blue
    var secondaryButtonColor: Color = .gray
    var navigationBarColor: Color = .black
}
