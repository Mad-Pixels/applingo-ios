import SwiftUI

// The default value for `survival` games.
let DEFAULT_SURVIVAL_LIVES = 3

// The default value for `timeAttack` games.
let DEFAULT_TIME_DURATION: Int = 90

// The special card chance.
let SPECIAL_CARD_CHANCE: Double = 0.2

// Range of possible values.
let AVAILABLE_LIVES: [Int] = [3, 4, 5, 6]
let AVAILABLE_TIME_DURATIONS: [Int] = [90, 120, 150]
