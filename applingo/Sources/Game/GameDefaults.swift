import SwiftUI

/// A constant representing the default number of lives for survival mode.
let DEFAULT_SURVIVAL_LIVES = 3
/// A constant representing the default duration (in seconds) for time mode.
let DEFAULT_TIME_DURATION: TimeInterval = 120
