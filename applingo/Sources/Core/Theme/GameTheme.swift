import SwiftUI

/// A struct defining the color theme for a specific game type.
struct GameTheme {
    // MARK: - Properties
    /// The main color for the game theme.
    let main: Color
    /// A darker variant of the main color.
    let dark: Color
    /// A lighter variant of the main color.
    let light: Color
}
